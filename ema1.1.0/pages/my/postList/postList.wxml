<view class="background">
  <navigator class="cu-card dynamic no-card solid" hover-class="none" wx:for="{{posts}}" wx:key="list" for-item="item" url="../../postDetail/postDetail?id={{item.id}}&value={{index}}" wx:if="{{!item.deleted}}">
    <view class="cu-item shadow">
      <!-- <view class="cu-list menu-avatar">
        <view class="cu-item">
          <view class="cu-avatar round lg" style="background-image:url({{avatarUrl}});"></view>
          <view class="content flex-sub">
            <view>{{item.userVo.nickName}}</view>
            <view class="text-gray text-sm flex justify-between">
              {{item.occurTime}}
            </view>
          </view>
        </view>
      </view> -->
      <view class="text-content margin-top" style="font-size: 32rpx;">
        {{item.title}}
      </view>
      <!-- <view class="grid flex-sub padding-lr {{isCard?'col-3 grid-square':'col-1'}}">
        <view class="bg-img {{isCard?'':'only-img'}}" style="background-image:url(https://ossweb-img.qq.com/images/lol/web201310/skin/big10006.jpg);" wx:for="{{isCard?9:1}}" wx:key="{{index}}">
        </view>
      </view> -->
      <view class="text-gray text-sm padding padding-top-sm flex">
        <view class="info">
        <text class="cuIcon-attentionfill margin-lr-xs"></text> {{item.views}}
        </view>
        <view class="info">
        <text class="cuIcon-favorfill margin-lr-xs"></text> {{item.attentions}}
        </view>
        <view class="info">
        <text class="cuIcon-messagefill margin-lr-xs"></text> {{item.comments}}
        </view>
      </view>
    </view>
  </navigator>
</view>